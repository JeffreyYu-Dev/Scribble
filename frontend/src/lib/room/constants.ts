/** Fixed config for a round. The drawing kit lives in `components/room/tools.ts`. */

export const TURN_SECONDS = 80;
export const TOTAL_ROUNDS = 3;

/** How long the drawer has to pick a word before the room picks one for them. */
export const CHOOSE_SECONDS = 15;

/** How long the answer stays up between turns. */
export const REVEAL_SECONDS = 5;

/** How many words the drawer is dealt to choose between. */
export const WORD_CHOICES = 3;
